<div class="p-2">
    <div>
        <label for="">{{ $label }}</label>
    </div>
    <div>
        <input type="{{$type}}" name="{{$name}}" placeholder="{{$placeholder ? $placeholder : ''}}" value="{{$value}}">
    </div>
</div>